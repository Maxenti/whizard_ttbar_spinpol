diff --git a/cglenn.cc b/cglenn.cc
index 12f6fbc..55a1543 100644
Binary files a/cglenn.cc and b/cglenn.cc differ
diff --git a/scripts/production/__pycache__/production_common.cpython-313.pyc b/scripts/production/__pycache__/production_common.cpython-313.pyc
index 01af34c..4e8aa66 100644
Binary files a/scripts/production/__pycache__/production_common.cpython-313.pyc and b/scripts/production/__pycache__/production_common.cpython-313.pyc differ
diff --git a/scripts/production/audit_phase10A_production_definition.sh b/scripts/production/audit_phase10A_production_definition.sh
index 5bbab63..60e2aef 100755
--- a/scripts/production/audit_phase10A_production_definition.sh
+++ b/scripts/production/audit_phase10A_production_definition.sh
@@ -216,6 +216,10 @@ find \
   "$REPO/configs" \
   "$REPO/analysis_contracts" \
   "$CAMPAIGN_DIR" \
+  \( \
+    -path "$CAMPAIGN_DIR/phase10A_production_definition" -o \
+    -path "$CAMPAIGN_DIR/phase10A_production_definition/*" \
+  \) -prune -o \
   -type f \
   \( \
     -iname '*365*' -o \
@@ -231,6 +235,15 @@ find \
   | sort -u \
   | tee "$AUDIT_DIR/relevant_config_inventory.txt"
 
+if grep -Fq "$AUDIT_ROOT/" "$AUDIT_DIR/relevant_config_inventory.txt"; then
+  echo "PHASE10A_AUDIT_SELF_CAPTURE_STATUS=FAIL"
+  echo "ERROR: audit output was discovered as an input configuration"
+  grep -Fn "$AUDIT_ROOT/" "$AUDIT_DIR/relevant_config_inventory.txt"
+  exit 1
+fi
+
+echo "PHASE10A_AUDIT_SELF_CAPTURE_STATUS=PASS"
+
 while IFS= read -r file; do
   [[ -f "$file" ]] || continue
   relative="${file#"$REPO"/}"
diff --git a/scripts/showering/validate_showering.py b/scripts/showering/validate_showering.py
index e8013aa..77f32ed 100755
--- a/scripts/showering/validate_showering.py
+++ b/scripts/showering/validate_showering.py
@@ -6,6 +6,7 @@ import argparse
 import csv
 import hashlib
 import json
+import time
 from collections import defaultdict
 from pathlib import Path
 from typing import Any
@@ -24,12 +25,52 @@ def load_config(path: Path) -> tuple[Path, dict[str, Any]]:
     return root, showering
 
 
-def sha256(path: Path) -> str:
-    digest = hashlib.sha256()
-    with path.open("rb") as stream:
-        for block in iter(lambda: stream.read(1024 * 1024), b""):
-            digest.update(block)
-    return digest.hexdigest()
+def sha256(
+    path: Path,
+    attempts: int = 5,
+    initial_delay_s: float = 5.0,
+) -> str:
+    """Return SHA256 with bounded retries for transient EOS read errors."""
+
+    last_error: OSError | None = None
+
+    for attempt in range(1, attempts + 1):
+        digest = hashlib.sha256()
+
+        try:
+            with path.open("rb") as stream:
+                for block in iter(
+                    lambda: stream.read(1024 * 1024),
+                    b"",
+                ):
+                    digest.update(block)
+
+        except OSError as exc:
+            last_error = exc
+
+            if attempt >= attempts:
+                break
+
+            delay_s = initial_delay_s * attempt
+
+            print(
+                "WARNING: checksum read failed; "
+                f"attempt={attempt}/{attempts} "
+                f"path={path} "
+                f"error={exc}; "
+                f"retrying_in={delay_s:.0f}s"
+            )
+
+            time.sleep(delay_s)
+            continue
+
+        return digest.hexdigest()
+
+    raise RuntimeError(
+        "Unable to checksum file after "
+        f"{attempts} attempts: {path}; "
+        f"last_error={last_error}"
+    ) from last_error
 
 
 def count_hepmc_events(path: Path) -> int:
